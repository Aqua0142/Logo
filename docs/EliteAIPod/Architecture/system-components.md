---
project_id: d32b5dee-31cc-444c-826f-355b186afead
stage: architecture
agent: solution_architect
deliverable: System-Components
generated_at: "2026-08-10T11:37:04.740695+00:00"
---

# System Components — lab-results-dashboard

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-10T11:37+00:00.

## ReactSPA (service)

Dark-mode React single-page application serving all user-facing views: Login, Data Entry (Lab Technician), My Results (Patient), and role-aware navigation bar.

**Responsibilities:**
- Render Login page with email/password form and client-side validation
- Render Data Entry form for Lab Technicians with field-level validation
- Render My Results table with conditional out-of-range highlighting (WCAG AA contrast)
- Decode JWT role claim and conditionally render navigation links per role
- Implement AuthGuard route protection with back-navigation prevention via history.replaceState
- Handle empty states and user-friendly error messages for all data fetching failures
**Depends on:** FastAPIServer**Communication protocol:** HTTPS/REST
**Port:** 5173
## FastAPIServer (service)

Modular monolith backend exposing REST API endpoints for authentication, session management, and blood work data operations. Enforces RBAC on every protected route.

**Responsibilities:**
- Serve POST /api/v1/auth/login — credential validation, Argon2 verification, JWT issuance
- Serve POST /api/v1/auth/logout — server-side session invalidation via sessions table
- Serve POST /api/v1/blood-work — Lab Technician RBAC, numeric precision storage
- Serve GET /api/v1/blood-work/my-results — Patient RBAC, JOIN with reference_ranges, is_out_of_range computation
- Validate JWT on every protected request and enforce role-based 403 responses
- Return structured JSON error envelopes; never expose stack traces
**Depends on:** PostgreSQLDatabase**Communication protocol:** HTTPS/REST
**Port:** 8000
## PostgreSQLDatabase (database)

PostgreSQL 16 database storing users, sessions, blood_work_results, and reference_ranges. PHI fields encrypted at rest using AES-256 via pgcrypto or SQLAlchemy TypeDecorator.

**Responsibilities:**
- Persist users with Argon2 password hashes
- Store session tokens for server-side logout invalidation
- Store blood_work_results with NUMERIC precision for lab values
- Store reference_ranges with seeded standard medical values
- Enforce foreign key constraints (patient_id → users, created_by → users)
- Apply AES-256 encryption at rest on PHI columns (patient_id encrypted FK, value)
**Communication protocol:** TCP/asyncpg
**Port:** 5432
## AuthModule (service)

Internal FastAPI module encapsulating all authentication and authorization logic: password hashing, JWT issuance/validation, RBAC middleware, and session management.

**Responsibilities:**
- Hash and verify passwords using Argon2 (passlib)
- Issue signed JWTs with user_id and role claims, 8-hour expiry
- Validate JWTs on every protected request via FastAPI Depends
- Enforce role-based access control, returning 403 for role mismatches
- Insert and invalidate session records for server-side logout
**Depends on:** PostgreSQLDatabase**Communication protocol:** in-process

## BloodWorkModule (service)

Internal FastAPI module encapsulating blood work record creation, retrieval, and reference range comparison logic.

**Responsibilities:**
- Validate and persist blood work records with NUMERIC precision
- Query blood_work_results joined with reference_ranges to compute is_out_of_range
- Enforce that only Lab Technicians can write blood work records
- Enforce that only the authenticated Patient can read their own results
- Seed reference_ranges table with standard clinical values on first migration
**Depends on:** PostgreSQLDatabase, AuthModule**Communication protocol:** in-process

