---
project_id: d32b5dee-31cc-444c-826f-355b186afead
stage: architecture
agent: solution_architect
deliverable: Data-Models
generated_at: "2026-08-10T11:37:09.428310+00:00"
---

# Data Models — lab-results-dashboard

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-10T11:37+00:00.

## User

Represents an authenticated user of the system. Roles are: patient, lab_technician, administrator. Accounts are pre-provisioned; no self-registration.
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `email` | VARCHAR(255) | no | — | UNIQUE, NOT NULL |
| `password_hash` | TEXT | no | — | NOT NULL |
| `role` | VARCHAR(32) | no | — | NOT NULL, CHECK role IN ('patient','lab_technician','administrator') |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- idx_users_email

**Relationships:**
- one_to_many → Session
- one_to_many → BloodWorkResult
- one_to_many → BloodWorkResult

_Used by: /api/v1/auth/login, /api/v1/auth/logout, /api/v1/blood-work, /api/v1/blood-work/my-results_
## Session

Server-side session tracking for JWT invalidation on logout. Each login creates a session entry; logout removes it, enabling server-side token denylist behavior.
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `token_jti` | VARCHAR(255) | no | — | UNIQUE, NOT NULL |
| `user_id` | UUID | no | — | NOT NULL, REFERENCES users(id) ON DELETE CASCADE |
| `expires_at` | TIMESTAMPTZ | no | — | NOT NULL |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- idx_sessions_token_jti
- idx_sessions_user_id

**Relationships:**
- many_to_one → User

_Used by: /api/v1/auth/login, /api/v1/auth/logout_
## BloodWorkResult

Individual blood work test result for a patient. PHI fields (patient_id association, value) are encrypted at rest using AES-256. The value column uses NUMERIC type to preserve exact decimal precision.
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `patient_id` | UUID | no | — | NOT NULL, REFERENCES users(id) |
| `test_name` | VARCHAR(255) | no | — | NOT NULL |
| `value` | NUMERIC(10,4) | no | — | NOT NULL |
| `units` | VARCHAR(50) | no | — | NOT NULL |
| `collected_at` | TIMESTAMPTZ | no | — | NOT NULL |
| `created_by` | UUID | no | — | NOT NULL, REFERENCES users(id) |
| `created_at` | TIMESTAMPTZ | no | NOW() | NOT NULL |

**Indexes:**
- idx_bwr_patient_id
- idx_bwr_patient_collected

**Relationships:**
- many_to_one → User
- many_to_one → User

_Used by: /api/v1/blood-work, /api/v1/blood-work/my-results_
## ReferenceRange

Hard-coded standard medical reference ranges for blood work tests, seeded via migration. Used for computing is_out_of_range in patient results queries.
| Field | Type | Nullable | Default | Constraints |
|---|---|---|---|---|
| `id` | UUID | no | gen_random_uuid() | PRIMARY KEY |
| `test_name` | VARCHAR(255) | no | — | UNIQUE, NOT NULL |
| `min_value` | NUMERIC(10,4) | yes | — | — |
| `max_value` | NUMERIC(10,4) | yes | — | — |
| `units` | VARCHAR(50) | no | — | NOT NULL |

**Indexes:**
- idx_reference_ranges_test_name


_Used by: /api/v1/blood-work/my-results_
