---
project_id: d32b5dee-31cc-444c-826f-355b186afead
stage: architecture
agent: solution_architect
deliverable: Technology-Stack
generated_at: "2026-08-10T11:37:02.461741+00:00"
---

# Technology Stack — lab-results-dashboard

> Generated by Elite AI Pod's Solution Architect agent on 2026-08-10T11:37+00:00.

| Category | Technology | Version | Rationale |
|---|---|---|---|
| backend | FastAPI | 0.111 | FastAPI offers async request handling, automatic OpenAPI docs, type-safe request/response via Pydantic, and native support for dependency injection — all essential for enforcing RBAC middleware and validating blood work data with numeric precision. Its performance characteristics easily meet the sub-500ms API response NFR. |
| database | PostgreSQL | 16 | PostgreSQL's NUMERIC type (arbitrary precision decimal) prevents floating-point rounding errors for blood work values — a critical data integrity requirement. Its pgcrypto extension supports AES-256 column-level encryption for PHI fields at rest. Row-level security and robust FK constraints enforce patient data isolation. |
| frontend | React | 18 | React 18 with TypeScript provides a mature component model for building the dark-mode UI with conditional formatting. Its rich ecosystem (React Query for server state, React Hook Form for validated data entry) accelerates implementation of the blood work entry form and patient results table within the sprint timeline. |
| auth | python-jose + passlib[argon2] | 3.3 / 1.3 | python-jose handles JWT signing/verification with RS256 or HS256. passlib with Argon2 satisfies the critical NFR for strong modern password hashing with salting. Together they implement the full JWT lifecycle: issue on login, verify on every request, invalidate on logout via server-side session denylist. |
| orm | SQLAlchemy | 2.0 | SQLAlchemy 2.0 async engine integrates natively with FastAPI's async context. Its TypeDecorator system allows transparent AES-256 encryption/decryption on PHI columns (patient_id, value). Alembic migrations provide deterministic schema evolution. |
| frontend_tooling | Vite + TypeScript + Tailwind CSS | 5 / 5.4 / 3.4 | Vite's dev server and optimized builds achieve the sub-1.8s FCP NFR. TypeScript catches contract mismatches between frontend and backend. Tailwind CSS with a dark-mode configuration and carefully validated contrast tokens satisfies the WCAG 2.1 AA accessibility NFR for the dark UI. |
| migration | Alembic | 1.13 | Alembic is the canonical SQLAlchemy migration tool, providing version-controlled schema migrations with upgrade/downgrade support. Generates migration scripts from model diffs, ensuring the blood_work_results and reference_ranges tables are deployed deterministically. |

**backend — alternatives considered:** Django REST Framework, Flask + Marshmallow, Node.js/Express
**database — alternatives considered:** MySQL 8, SQLite (rejected: not suitable for encrypted PHI), MongoDB (rejected: NUMERIC precision not native)
**frontend — alternatives considered:** Vue 3, Angular 17, Svelte
**auth — alternatives considered:** PyJWT + bcrypt, Authlib, Auth0 (rejected: external dependency for prototype)
**orm — alternatives considered:** Tortoise ORM, SQLModel, raw asyncpg
**frontend_tooling — alternatives considered:** Create React App (rejected: slow builds), Next.js (overkill for prototype), Webpack + SASS
**migration — alternatives considered:** Flyway, Django migrations (not applicable), manual SQL scripts
